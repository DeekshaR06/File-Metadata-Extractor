# Test files used during local development (may contain personal metadata)
test.pdf
test.docx
test.png
test.txt
*final.docx
protocol.pdf

# Lambda layer dependencies (large, rebuildable from requirements — not source code)
lambda-layer/
*.zip

# Python cache
__pycache__/
*.pyc

# OS/editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/

# AWS setup helper files (not part of the application itself)
Lifecycle.json
